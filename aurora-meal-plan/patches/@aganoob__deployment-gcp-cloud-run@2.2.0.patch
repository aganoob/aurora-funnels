diff --git a/dist/index.js b/dist/index.js
index 92c75b7415dfbe4ceeadc85cbb4b3fefc5b9426a..a7b779cb96bd59a83d815280c3f2b7a31135eb90 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -19,14 +19,15 @@ var gcpAnalyticsDelivery = (config = {}) => ({
   ...config
 });
 var safeName = (value) => value.toLowerCase().replace(/[^a-z0-9-]/g, "-").replace(/-+/g, "-").replace(/^-|-$/g, "").slice(0, 48);
+var serviceAccountName = (prefix, suffix) => `${prefix.slice(0, 30 - suffix.length - 1).replace(/-+$/, "")}-${suffix}`;
 function gcpResourceNames(environment, target) {
   const prefix = safeName(`${target.service}-${environment}`);
   return {
     repository: "shipflow",
-    buildServiceAccount: `${prefix}-build`,
-    funnelServiceAccount: `${prefix}-funnel`,
-    deliveryServiceAccount: `${prefix}-delivery`,
-    tasksServiceAccount: `${prefix}-tasks`,
+    buildServiceAccount: serviceAccountName(prefix, "build"),
+    funnelServiceAccount: serviceAccountName(prefix, "funnel"),
+    deliveryServiceAccount: serviceAccountName(prefix, "delivery"),
+    tasksServiceAccount: serviceAccountName(prefix, "tasks"),
     deliveryService: `${prefix}-delivery`,
     tasksQueue: `${prefix}-delivery`,
     firestoreDatabase: safeName(`shipflow-${environment}`),
